# Test Environment Configuration
# These are mock values for testing - tests will use localStorage fallback

NEXT_PUBLIC_SUPABASE_URL=https://test.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=test-anon-key
NODE_ENV=test