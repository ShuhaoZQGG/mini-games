import GamePlaceholder from '../GamePlaceholder'

export default function MagicSquare() {
  return (
    <GamePlaceholder 
      title="Magic Square"
      description="Arrange numbers so each row, column, and diagonal sum equally"
      category="Puzzle Game"
    />
  )
}
